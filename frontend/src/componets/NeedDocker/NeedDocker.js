import React from 'react';

const NeedDocker = (state) => {

	return (
		<div>
			<img src='img/Docker.png' className='App-logo' alt='docker '/>
			<p>Need install Docker</p>
			<a href="https://docs.docker.com/install/" target="_blank" rel='noopener noreferrer'>Instruction</a>
		</div>

	);
};

export default NeedDocker;
